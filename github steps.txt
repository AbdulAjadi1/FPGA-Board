github steps:

-download git to pc
-clone project repository to location on pc
-navigate to clone location in git CMD

- ***Always pull current version first***
-pulls any changes that have been made by other members 

git pull origin main  			# Always pull the latest changes first
git add .             			# Stage all changes
git commit -m "Updated feature XYZ"     # Commit with a meaningful message
git push origin main  			# Push to the remote repository
